# GitHub Pages 部署

这个项目是纯静态结构，非常适合 GitHub Pages。

## 上传文件

把项目完整上传到仓库根目录，确保至少包含：

```text
index.html
css/
js/
docs/
images/
```

## 开启 Pages

在仓库 Pages 设置中选择用于发布的分支和根目录。

## 更新方式

后续日常更新只需要：

1. 修改 `docs/xxx.md` 正文；或
2. 新增 Markdown 后同步修改 `index.html` 中的导航数组。
